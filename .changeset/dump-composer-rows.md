---
type: feat
summary: Thought Dump composer starts as a single line
---

The composer textarea now defaults to one row (still resizable / auto-grows as
you type) instead of three, so an empty composer is compact.
